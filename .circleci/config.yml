version: 2.1

jobs:
  build-and-test:
    working_directory: ~/repo
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run: mvn clean install

  workflows:
    main:
      jobs:
        - build-and-test
